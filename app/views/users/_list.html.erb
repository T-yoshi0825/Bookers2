<div class="container">
    <div class="row">
      <div class="col-xs-3">

        <h1>User info</h1>
             <div class="profile_image">
              <td><%= attachment_image_tag users, :profile_image, size: "100x100", format: 'jpeg', fallback: "no_image.jpg" %></td>
             </div>
          <table>
              <thead></thead>
              <body>
                <tr>
                  <td>name</td>
                  <td><%= users.name %></td>
                </tr>
                <tr>
                  <td>introduction</td>
                  <td></td>
                  <td><%= users.introduction %></td>
                </tr>
              </body>
          </table>
           <%= link_to edit_user_path(current_user.id) do %>
           <span class="btn btn-success btn-block glyphicon glyphicon-wrench" aria-hidden="true"></span>
           <% end %>
          <br>

        <h1>New book</h1>
          <% if books.errors.any? %>
            <div class="alert alert-warning">
              <% if books && books.errors.any? %>
              <%= books.errors.count %>errors prohibited this obj flom being save:<br>
              <% books.errors.full_messages.each do |massage| %>
              <%= massage %>
              <% end %>
              <% end %>
            </div>
          <% end %>

          <%= form_for(books) do |f| %>
            <h4>Title</h4>
          <%= f.text_field :title, :class =>"form-control" %>

            <h4>Opinion</h4>
          <%= f.text_area :body, :class =>"form-control" %>
          <br>
          <section>
            <button type="submit" class="btn btn-primary btn-block">Create Book</button>
          </section>
          <% end %>
      </div>